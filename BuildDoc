TEST_VAR="wow this is working!!!"

[main]
echo "val of var TEST: $TEST_VAR" # IT WORKS!!!
# %goto(priv)

[.priv] # '.' makes this task private, meaning it can't be run from the command line, only via another task.
echo "echoed from private task!"
